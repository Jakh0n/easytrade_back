# Server
NODE_ENV=development
PORT=4000
# Comma-separated origins, or * for any
CORS_ORIGIN=*

# Database
MONGODB_URI=mongodb+srv://yoqubjonovjahongir10_db_user:tTtxtotkS3GJDYh9@cluster0.owxb9a6.mongodb.net/
tTtxtotkS3GJDYh9

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# OpenAI (optional — technical signals work without it)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Render / production keep-alive (optional locally)
# Render avtomatik RENDER_EXTERNAL_URL beradi; yoki qo'lda:
# APP_URL=https://your-app.onrender.com
